apiVersion: v1
kind: Service
metadata:
  name: {{ include "wavefront.fullname" . }}
  labels:
    app: {{ include "wavefront.name" . }}
    chart: {{ include "wavefront.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "wavefront.name" . }}
    release: {{ .Release.Name }}
